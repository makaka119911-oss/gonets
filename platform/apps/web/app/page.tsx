export default function Page() {
  return (
    <main>
      <h1>Гонец — веб-клиент</h1>
      <p>Запустите API (`npm run dev:api`), затем войдите на странице «Вход» и проверьте калькулятор и заказы.</p>
    </main>
  );
}
